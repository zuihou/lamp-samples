lamp:
  database:
    isSeata: false
    tenantDatabasePrefix: lamp_extend
  swagger:
    docket:
      demo:
        title: 演示服务
        base-package: top.tangyh.lamp.demo.controller

logging:
  org.springframework.cloud.alibaba.seata.web: debug

server:
  servlet:
    context-path:
  port: 8770



spring:
  cloud:
    nacos:
      discovery:
        metadata:
          grayversion: demo
